
# Netlify-style redirects for SPA routing
# This file can also be used as reference for OpenLiteSpeed configuration

# API routes should not be redirected
/api/* /api/:splat 200

# All other routes should serve index.html for SPA
/* /index.html 200
